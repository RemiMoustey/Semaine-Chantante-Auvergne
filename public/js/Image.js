/*
	Crée une image du diaporama d'explications avec les deux attributs obligatoires en HTML (src et alt) et une classe.
*/

class Image {

	constructor(src, alt, classHTML) {
		this.src = src;
		this.alt = alt;
		this.classHTML = classHTML;
	}
}