{% macro blockSong(title, paragraph, nameSrcSong) %}
        <div class="bloc-song">
            <h2>{{ title }}</h2>
            <p>{{ paragraph }}</p>
            <audio id="{{ nameSrcSong }}" src="./public/songs/{{ nameSrcSong }}.mp3" controls></audio>
        </div>
{% endmacro %}